apiVersion: v1
kind: Service
metadata:
  name: inventory
spec:
  ports:
  - port: 8080
    protocol: TCP
{{ if .Values.useProxy }}
    targetPort: 9090
{{ end }}
  selector:
    app: inventory
  type: LoadBalancer
